# Domain Scope

This example packet has no final domains yet.

Use this file to list possible domains touched by a real idea.

## Possible domains

```txt
domain name: input
why this idea touches it: every arcade idea needs a simple input model
existing kit that may own it:
possible new kit if needed:
what the domain owns: input mapping and action descriptors
what the domain does not own: game scoring truth
state:
actions:
events:
descriptors:
validation rules:
```

```txt
domain name: scoring
why this idea touches it: arcade ideas usually need score chase or progression
existing kit that may own it:
possible new kit if needed:
what the domain owns: score rules and score events
what the domain does not own: rendering score text directly
state:
actions:
events:
descriptors:
validation rules:
```

## Required domains

```txt
-
```

## Optional domains

```txt
-
```

## Rejected domains

```txt
-
```
