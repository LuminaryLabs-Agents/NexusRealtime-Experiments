# Subdomain Scope

This example packet has no final subdomains yet.

Use this file to list possible subdomains touched by a real idea.

## Possible subdomains

```txt
subdomain name: action-input-mapping
parent domain: input
why this idea touches it: arcade controls need clear action mapping
existing kit that may own it:
possible new kit if needed:
what the subdomain owns: mapping physical input to validated actions
what the subdomain does not own: game-specific scoring outcomes
state:
actions:
events:
descriptors:
validation rules:
```

```txt
subdomain name: score-combo-window
parent domain: scoring
why this idea touches it: many arcade ideas use timing-based score reward
existing kit that may own it:
possible new kit if needed:
what the subdomain owns: combo timing and score multiplier windows
what the subdomain does not own: rendering combo text
state:
actions:
events:
descriptors:
validation rules:
```

## Required subdomains

```txt
-
```

## Optional subdomains

```txt
-
```

## Rejected subdomains

```txt
-
```
