# ProtoKit Proof

This example packet is not proving a real game yet.

It shows where the ProtoKit proof should live.

## Core ProtoKit question

Can the idea be described in terms of domains, subdomains, services, kits, state, actions, events, and descriptors?

## Existing ProtoKits that may apply

```txt
kit: none selected yet
domain:
why it applies:
what it owns:
what it should not own:
```

## Possible new ProtoKits

```txt
proposed kit: none yet
domain:
subdomain:
service responsibility:
inputs:
outputs:
events:
state:
validation rules:
```

## Domain update first audit

```txt
Can an existing DSK own this behavior? unknown
Could this be a config/preset instead of a new kit? unknown
Could this be a bridge inside the experiment instead of a new domain? unknown
Does the behavior belong to a broad reusable domain? unknown
Does the behavior only exist for this one game? unknown
```

## Renderer boundary

The renderer should remain presentation-only.

## Runtime proof

```txt
smoke test:
deterministic replay:
state snapshot:
action validation:
failure condition:
restart behavior:
```
