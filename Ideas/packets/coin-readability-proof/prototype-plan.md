# Prototype Plan

## Smallest prototype

```txt
load coin seed
create coin mesh descriptor
apply one material variant
apply highlight descriptor
emit LOD descriptors
emit readability report
```

## Success criteria

- Coin reads instantly at mid distance.
- Material variant is obvious.
- Highlight does not hide shape.
- Output is deterministic.
