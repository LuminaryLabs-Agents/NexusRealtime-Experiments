# Prototype Plan

## Smallest prototype

```txt
load button seed
create button mesh descriptor
apply plastic material
emit idle, pressed, and lit descriptors
emit wear overlay descriptor
emit budget report
```

## Success criteria

- Button reads as physical hardware.
- Pressed state is visible.
- Lit state is obvious but not overblown.
- Output is deterministic.
