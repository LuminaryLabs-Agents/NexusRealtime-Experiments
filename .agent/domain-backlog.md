# Domain Backlog

Track reusable domain opportunities and higher-level domains.

Rules:

- Prefer cumulative domains over one-off route features.
- Kit implementation belongs in ProtoKits.
- Experiments validate domains through routes, presets, bridges, docs, manifests, and tests.
- DSKs connect domains through resources, events, methods, snapshots, and descriptors.

Open findings should be appended here by scheduled tasks.

## 2026-06-23 Canonical Route Pruner higher-level domains

The route-pruning map keeps the current canonical set small because the reusable pressure is clustering above individual fantasy routes:

- Survey pressure: scan targets + zone fields + timed/resource pressure + hazards.
- Strategic pressure loop: defense map/slots + economy + build placement + waves/agents + combat + descriptors.
- Survival ecology: pickups/gear + horde cadence + hazards + resource pressure + outcome snapshots.
- Traversal/cargo pressure: checkpoints + cargo ledger + tether/vehicle contact + pressure deltas.
- Aerial/open traversal: flight input envelope + terrain/updraft windows + checkpoint ledger + camera descriptors.
- Field-engineer composition: scan/harvest/build/pressure/gate/cargo/beacon kit composition without turning every field-sim into a route.
- Action-defense-extraction: harvest/inventory/build/wave/core/portal state folded into one unified base route before further promotion.

ProtoKit opportunity: build executable smoke/replay fixtures around these higher-level lanes before promoting more seed/backlog routes to canonical status.

## 2026-06-23 Canonical Route Pruner import-gate backlog

The strategic-pressure import-wiring backlog is now satisfied for the first route-domain executable replay: Experiments can import Core `nexusrealtime` and ProtoKits `@luminarylabs/nexusrealtime-protokits` through package-level dev dependencies, and the Signal Bastion executable replay uses real generic-defense DSK aliases instead of route-local simulation copies.

## 2026-06-23 Cycle Report Main Push Planner backlog update

Next backlog item: reduce the remaining Signal Bastion browser-host compatibility facade seam without breaking the executable route replay or static bridge/spec/facade guards.

Scope for the next safe implementation pass:

- Keep reusable implementation in ProtoKits.
- Keep Experiments changes limited to browser-host bridge shrink, manifests, tests, and `.agent` memory.
- Prefer replacing host convenience calls with existing semantic DSK/session/presentation methods only when the browser route stays compatible.
- Keep `strategic-pressure-loop` as the only executable route-domain lane until another higher-level domain has a real reusable ProtoKit boundary.

Still-blocked backlog lanes:

- Survey pressure, traversal/cargo pressure, aerial/open traversal, survival ecology, field-engineer composition, and action-defense-extraction remain contract-only until their reusable DSK surfaces can be imported, ticked headlessly, and asserted through resources, events, methods, snapshots, and descriptors.
