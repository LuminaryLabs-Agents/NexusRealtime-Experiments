# DSK Boundary Notes

DSKs are layered domain communication surfaces. Use them to connect reusable domains through resources, events, methods, snapshots, and descriptors.

Reusable kit implementation belongs in ProtoKits. Experiments should use presets, bridges, manifests, docs, routes, and tests to validate those domains.

Each cycle should look for higher-level domains that emerge when existing domains combine.
