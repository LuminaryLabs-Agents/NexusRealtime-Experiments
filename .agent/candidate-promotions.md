# Candidate Promotions

Record route behavior that should become reusable ProtoKits.

## 2026-06-23 Cycle Report Main Push Planner promotion state

Promotion rule: route behavior should move toward ProtoKits only when it is renderer-agnostic, reusable across routes, and can communicate through resources, events, methods, snapshots, and descriptors with headless tick smoke or replay coverage.

Current strongest promotion proof:

- `signal-bastion` validates the `strategic-pressure-loop` higher-level domain through real Core plus ProtoKits generic-defense DSK aliases in the executable Node replay.
- The reusable implementation stays in ProtoKits; Experiments owns the route host, preset bridge, browser input bridge, manifests, specs, and tests.
- The browser host now consumes the narrower `generic-defense-aaa-dsk-bridge` and explicit DSK alias IDs, but still carries compatibility convenience facades. Those facades are a shrink target, not a Core-promotion surface.

Reusable ProtoKit candidates to keep promoting or preparing:

- `generic-pressure-loop-kit`.
- `generic-resource-loop-kit`.
- `generic-action-window-kit`.
- `generic-affordance-descriptor-kit`.
- `generic-defense-dsk-boundaries` atomic aliases: map, economy wallet, build placement, wave/agent director, combat resolver, session facade, and render descriptors.

Do not promote from Experiments yet:

- Survey-pressure route logic, traversal/cargo route logic, aerial traversal route logic, survival ecology route logic, field-engineer route logic, or action-defense-extraction route logic. These lanes have checked contracts but still lack executable reusable ProtoKit boundaries.
- Browser, renderer, DOM, Canvas, WebGL, Three.js, pointer lock, browser audio, asset loading, route-local randomization, and route-specific visual presentation.

Next promotion gate: prove another higher-level lane with an actual ProtoKit DSK surface before adding a second executable route-domain replay.
