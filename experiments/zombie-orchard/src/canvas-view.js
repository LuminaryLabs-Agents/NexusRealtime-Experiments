const n = (v, f = 0) => Number.isFinite(Number(v)) ? Number(v) : f;
const clamp = (v, a, b) => Math.max(a, Math.min(b, v));
const appleColor = (r) => r === "legendary" ? "#d36bff" : r === "rare" ? "#ffe06b" : r === "cursed" ? "#ad57ff" : "#df3f38";
const monsterColor = (m) => m.boss ? "#ff365f" : m.elite ? "#f0d27b" : m.archetypeId === "runner-zombie" ? "#a4f080" : "#87a45f";
function resize(canvas) { const dpr = Math.min(2, devicePixelRatio || 1); const w = Math.max(320, Math.floor(canvas.clientWidth * dpr)); const h = Math.max(240, Math.floor(canvas.clientHeight * dpr)); if (canvas.width !== w || canvas.height !== h) { canvas.width = w; canvas.height = h; } return { w, h }; }
export function createCanvasView(canvas) {
  const ctx = canvas.getContext("2d"); let cam = { x: 0, z: 20, zoom: 8.4 };
  const p = (q, s) => ({ x: s.w / 2 + (n(q?.x) - cam.x) * cam.zoom, y: s.h / 2 + (n(q?.z ?? q?.y) - cam.z) * cam.zoom });
  const label = (t, x, y) => { ctx.save(); ctx.font = "700 12px system-ui"; ctx.textAlign = "center"; ctx.lineWidth = 4; ctx.strokeStyle = "#000a"; ctx.strokeText(t, x, y); ctx.fillStyle = "#fff0d0"; ctx.fillText(t, x, y); ctx.restore(); };
  function dot(point, s, color, radius, text) { const q = p(point, s); ctx.fillStyle = color; ctx.beginPath(); ctx.arc(q.x, q.y, radius, 0, Math.PI * 2); ctx.fill(); if (text) label(text, q.x, q.y - 16); return q; }
  return { render(snapshot = {}) { const s = resize(canvas); const player = snapshot.player ?? { position: { x: 0, z: 0 } }; cam.x += (n(player.position?.x) - cam.x) * 0.09; cam.z += (n(player.position?.z, 20) - cam.z) * 0.09; cam.zoom = clamp(cam.zoom + ((snapshot.cameraZoom ?? 8.4) - cam.zoom) * 0.04, 4, 13); const grad = ctx.createRadialGradient(s.w / 2, s.h * 0.42, 0, s.w / 2, s.h / 2, Math.max(s.w, s.h) * 0.72); grad.addColorStop(0, "#252b12"); grad.addColorStop(0.58, "#101407"); grad.addColorStop(1, "#040403"); ctx.fillStyle = grad; ctx.fillRect(0, 0, s.w, s.h);
      const orchard = snapshot.orchard ?? {}; const halfW = n(orchard.width, 76) / 2, halfD = n(orchard.depth, 104) / 2; ctx.strokeStyle = "#a8774baa"; ctx.lineWidth = 3; ctx.setLineDash([8, 5]); const corner = p({ x: -halfW, z: -halfD }, s); ctx.strokeRect(corner.x, corner.y, n(orchard.width, 76) * cam.zoom, n(orchard.depth, 104) * cam.zoom); ctx.setLineDash([]);
      for (const lane of orchard.fogLanes ?? []) { const q = p(lane.center, s); ctx.fillStyle = "rgba(151,163,190,.16)"; ctx.beginPath(); ctx.ellipse(q.x, q.y, n(lane.width, 4) * cam.zoom, n(lane.length, 60) * cam.zoom * .32, 0, 0, Math.PI * 2); ctx.fill(); }
      for (const zone of orchard.hauntedZones ?? []) { const q = p(zone.position, s); ctx.strokeStyle = zone.id === orchard.haunting?.activeZoneId ? "#ff335b88" : "#8043a044"; ctx.beginPath(); ctx.arc(q.x, q.y, n(zone.radius, 7) * cam.zoom, 0, Math.PI * 2); ctx.stroke(); }
      for (const b of orchard.barnLandmarks ?? []) { const q = p(b.position, s); ctx.fillStyle = b.kind === "shed" ? "#503724dd" : "#5c231edd"; ctx.fillRect(q.x - n(b.size?.x, 8) * cam.zoom / 2, q.y - n(b.size?.z, 7) * cam.zoom / 2, n(b.size?.x, 8) * cam.zoom, n(b.size?.z, 7) * cam.zoom); label(b.kind, q.x, q.y - 18); }
      for (const row of orchard.treeRows ?? []) for (const tree of row.trees ?? []) { const q = p(tree.position, s); ctx.fillStyle = "#142810b8"; ctx.beginPath(); ctx.arc(q.x, q.y, n(tree.canopyRadius, 2.8) * cam.zoom, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = "#3b2615"; ctx.beginPath(); ctx.arc(q.x, q.y, Math.max(2, n(tree.radius, .7) * cam.zoom), 0, Math.PI * 2); ctx.fill(); }
      for (const w of snapshot.weapons?.pickups ?? []) if (w.active !== false) { const q = dot(w.position, s, "#261e15", 7, snapshot.nearestWeapon?.id === w.id ? "E" : ""); ctx.strokeStyle = "#ffd168"; ctx.strokeRect(q.x - 7, q.y - 7, 14, 14); }
      for (const a of orchard.activeApples ?? []) dot(a.position, s, appleColor(a.rarity), snapshot.nearestApple?.id === a.id ? 7 : 5, snapshot.nearestApple?.id === a.id ? "E" : "");
      for (const m of snapshot.monsters ?? []) dot(m.position, s, monsterColor(m), m.boss ? 15 : m.elite ? 10 : 7, m.boss ? "KEEPER" : "");
      const pq = dot(player.position, s, snapshot.danger ? "#fff2b0" : "#f8e9ba", 9, ""); const f = player.facing ?? { x: 0, z: -1 }; ctx.strokeStyle = "#fff5dc"; ctx.lineWidth = 3; ctx.beginPath(); ctx.moveTo(pq.x, pq.y); ctx.lineTo(pq.x + f.x * 16, pq.y + f.z * 16); ctx.stroke();
      if (snapshot.paused || snapshot.gameOver) { ctx.fillStyle = "#0008"; ctx.fillRect(0, 0, s.w, s.h); ctx.font = "700 42px system-ui"; ctx.textAlign = "center"; ctx.fillStyle = "#fff0d0"; ctx.fillText(snapshot.gameOver ? "THE ORCHARD CLAIMED YOU" : "PAUSED", s.w / 2, s.h / 2); } return snapshot; } };
}
